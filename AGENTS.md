# TidyNest（拾净）

原生 macOS 的 Mole 配套工具。技术路线：SwiftUI＋必要的 AppKit＋独立 Mole 适配层。

## 当前范围

- M1 已完成：Mole 检测、应用列表与详情、磁盘查询、查询错误与取消。
- 磁盘地址栏使用原生可点击分级路径，单击上层目录直接读取，当前目录不重复查询；长路径仅展示最近四层，左侧「…」菜单按完整名称列出所有祖先至根目录。读取/取消收尾/执行/退出期间沿用 canAnalyzeDisk 门槛，根目录禁用上层入口；保留原向上、刷新、Finder 操作及稳定详情分栏。磁盘列表单击查看详情，双击文件夹行进入下一层，普通文件双击不读取；使用系统列表 primaryAction 按回调 ID 确认目标，复用原查询重入守卫，箭头和右键菜单保留。
- 侧栏顺序为「应用、清理、磁盘、操作记录」。每次启动固定进入「应用」页，不记住上次页面，不自动扫描。应用列表只恢复上次成功结果并检测 Mole，不自动查询；点击「读取应用」或「刷新列表」后才查询；刷新期间保留旧列表、搜索与选中项，并显示更新时间。失败或取消保留旧结果；缓存中的应用可直接发起移除检查，无需先刷新。Mole 返回 `unknown` 标识时，首次检查和重新检查均在现有维护任务内使用安全单项读取补全当前标识，再交给引擎独立核验；保持所选外层路径，不改列表缓存，已有真实标识冲突不自动替换。列表只用于选择目标，维护引擎独立核验当前路径、标识、安装集合及保护规则；同类任务同类任务忙碌或退出收尾期间仍禁止重入。缓存文件为 `~/Library/Caches/TidyNest/applications.json`。
- 应用移除执行收尾后，按已确认选择的本体项目 ID 与精确路径同步列表和缓存，清除已移除项的详情与单项刷新状态，保留搜索和完整列表更新时间，不自动全量扫描。整体取消、部分失败或通信中断仍保留已确认的逐项移除事实；本体失败、保留或待核对时不移除列表条目。缓存保存失败显示提示，不把实际移除改判失败；读取历史不会回放移除以免误隐藏后来重装的应用。
- 应用详情支持「刷新此应用」：直接读取所选应用的当前名称、Bundle ID 与包体积，只替换对应记录；来源和卸载名称沿用列表记录，维护引擎仍独立复核。单项刷新仅用于更新显示信息，保留完整列表时间、其他记录、搜索和选中状态；失败/取消保留旧信息，仍可独立检查移除计划，最终资格由维护引擎决定。已知 iPhone/iPad 包装布局可单独刷新信息：只读 `WrappedBundle` 的包内相对链接文本，再以无链接路径读取 `Wrapper/<名称>.app/Info.plist`，体积仍统计外层应用包；维护支持范围见下方包装应用规则。顶层/祖先/载荷路径链接仍不支持。单项刷新省去界面全量查询，移除计划中的安装集合/保护规则核验仍保留。
- 普通 Mole 检测在侧栏显示进度与取消，不显示顶部加载横幅；启动主内容保持原位。缺失、失败、不支持版本、取消完成后的重试提示及安装进度仍在主区显示，检测完成前不提前启用查询。
- Mole 缺失时显示安装按钮；用户点击后下载固定 1.53.0，校验、安装并重新检测。支持阶段进度、取消与失败重试。
- 清理检查/应用移除计划检查可与应用列表刷新、单项刷新或磁盘读取双向并行；记录读取使用独立任务与 Bridge 客户端，也可同时进行。各自取消与结果互不干扰，检查期间可删除或清空确认时的精确记录集合，稍后产生的新记录保留。应用与磁盘查询之间仍串行，同类任务及取消收尾不重入；同一时间只生成一个维护计划。实际移入废纸篓与保护设置等待查询/记录读取及取消收尾；执行期间不刷新事务记录或删除页面记录。退出先禁止新请求并取消各只读任务，再分别等候收尾；不放宽引擎规则。
- 维护检查使用横向流动进度条，显示当前应用名称、序号与最终复核阶段，不猜测整体百分比；执行按已选项目的实际结果显示已处理数量，失败和保留也计入进度，收到最终结果前不会标为完成。取消入口与逐项结果保留。
- 检查提示标题整行均可点击展开/收起，并显示操作文字与辅助功能状态；新计划默认折叠。展开内容按实际高度布局，短提示不占满空白，超过 120 点才滚动；路径和原因按条紧凑分组，完整文本可选择。移除计划本体仍默认勾选，但允许取消；取消本体同步清除依赖文件选择和旧确认，重新勾选不恢复残留选择。空选择或缺少任务必选项不能确认，引擎与协议的必选/依赖约束不变。
- 确认弹窗的应用项复用系统读取的应用图标，避免空轮廓被误认为复选框；清单仅用于最终核对，需要修改勾选时点击「返回调整选择」回到计划页。macOS 15.4+ 显式允许确认弹窗期间退出 App，由现有退出代理等待任务收尾；退出不触发未确认的移除。旧系统保留原视图以兼容 macOS 14。
- 占用检查区分应用/后台进程运行与外部程序访问，显示进程名称、PID、访问方式和具体路径；计划页提供「重新检查此应用」，重新生成同一目标的计划并清除旧选择与确认，维护忙碌/退出期间仍禁止重入。结果是检查时的快照，不自动刷新或关闭占用程序。整包本体仅容忍可确认外部进程以普通数字文件描述符只读、无锁打开的普通文件；目录、写入、锁定、映射与未知访问继续保留，缓存/日志不放宽。
- 已知 iPhone/iPad 包装应用支持整个外层包的移除计划（`WrappedBundle → Wrapper/<名称>.app`），并沿用完整包快照与全部保护。唯一安装可列出全局精确 Bundle ID 缓存/日志，以及可完整核验唯一归属的单应用容器 `Data/Library/Caches`、`Data/Library/Logs` 普通文件，均默认不选。Documents、Application Support、Preferences、tmp 与 Group Containers 不列入候选；不删除容器根或元数据。容器归属不可读/未知/重复时保留容器文件，仍允许独立核验通过的本体；已列出的候选枚举不完整或最终身份变化则计划不可执行。容器读取不申请额外权限。
- 多版本普通应用按所选路径生成本体移除计划；同 Bundle ID 的共享缓存和日志保留。Xcode 仅支持本体移除，开发数据保留，其他 Apple 应用仍受保护。应用包只读快照接受当前用户或 root 属主，组写仅兼容 root:wheel；完整核验全部位于包内的硬链接，体积按 inode 去重。普通缓存/日志仍限当前用户且无硬链接。快照以紧凑数组保存全部身份字段（含属组），仍受 32 MiB 上限约束。身份、安装集合、运行状态、权限、链接和同卷核验仍生效；普通处理不提权，权限受限本体仅走下述明确确认的系统通道。
- 应用本体先预检系统 DELETE + ADD_SUBDIRECTORY 权限；无链接父目录 FD 与具体包名核验，普通项继续同卷私有暂存、排他移动。用户已授权扩展系统移除：仅 EACCES 且其他身份、保护、运行状态和完整快照均通过的本体标记「需要系统授权」并允许勾选；确认后以固定 Finder 废纸篓命令处理。生成引用前从持有 FD 读取不透明资源标识，直接复核 CF 文件引用的资源标识和 inode；不使用路径 alias、不拼接 shell，FD 保留至回执。完整复核返回位置与对象后才处理已选缓存/日志；取消或拒绝且源完整则保留，超时/缺少回执/身份变化记待核对，不自动重试。应用及 Bridge 不以 root 运行、不改应用权限、不收集密码；仅此确认操作可能请求 Finder 自动化及系统移除授权。缓存、日志和受保护项目不走系统授权，其他拒绝原因继续锁定。
- 操作记录覆盖应用列表/单项刷新、磁盘读取、清理与移除计划检查、实际移除；记录结束时间、类型、目标和结果，失败/取消亦记录。启动检测、缓存恢复、搜索/选中和历史刷新不记。页面快照为 `~/Library/Application Support/TidyNest/operation-history.json`，有界私有原子保存；旧引擎执行结果按 runID 导入去重，过去未记录的查询不补录。单条删除与清空均确认精确 ID 集合，执行记录删除标记防止重开/刷新再导入；只清页面，不删引擎审计、防重放凭据或实际文件。损坏文件不覆盖，当前会话结果保留并显示存储错误。应用、磁盘与操作记录详情使用稳定分栏宿主，避免选择引起宽度跳动。
- 缓存/日志范围内的数据库（含 SQLite 文件头、`.db`、`-wal`、`-shm`）及会话、Cookie、凭据等内容性风险改为可选文件，默认不勾选，由用户判断后明确确认。详情与确认清单说明可能退出登录、丢失本地状态或出现应用异常，并提示核对数据库配套文件；未选文件保留。Documents、Application Support、Preferences 等目录未扩展；编译模型、长期保护、白名单、归属、占用、身份、权限、链接与同卷校验继续生效。当前规则版本为 `mole-1.53.0-subset-5`，旧计划须重新检查。
- M2 已实现：固定 Mole 1.53.0 的规则移植子集，支持计划预览、明确确认、逐项移入废纸篓、取消、本地记录与长期保护。验收结果及未测边界见 `docs/m2-plan.md`。
- 完整设计见 `docs/design.md`，M1 验收见 `docs/m1-plan.md`，当前实现计划见 `docs/m2-plan.md`。
- macOS 14+、Swift 6、Swift Package Manager；不引入外部包依赖。

## 版本控制与发布

- 2026-09-08 起使用本地 Git，默认分支为 `main`；构建产物、验收 fixture、应用备份和本地环境文件不提交。
- 用户计划后续发布公开的 `tidy-nest` GitHub 仓库；当前仅整理本地版本，功能打磨完成后由用户决定推送。未获新的明确指令前，不创建远程仓库、不配置远程地址、不推送。
- 项目首页和构建说明见 `README.md`。对外发布前需确认项目级开源许可并保留现有 Mole 来源及 GPL 材料。
- 首次纳入 Git 的文件已在仅由暂存区导出的干净副本完成编译和默认 166 项测试，3 项 opt-in 未启用。上游 6 份审计摘录摘要核对一致，保留原文末尾空行。

## 代码约定

- `Sources/TidyNestCore`：数据模型、查询白名单、子进程适配；不依赖 SwiftUI。
- `ApplicationRefreshService`：使用有界、无链接的普通或已知包装布局 Info.plist 读取和固定 `/usr/bin/du -sk` 统计外层单包占用，沿用子进程超时/取消；不会调用完整 Mole 应用列表。
- `RuntimeInspectionService`：固定 ps/lsof 只读核验。lsof 采用 NUL 字段分隔，完整读取进程/描述符/读写/锁/类型/路径；C 风格转义按 UTF-8 字节仅解码一次，再验证目标范围，不依赖启动 locale。缺失、重复、损坏或越界记录均不放行。
- `MoleInstaller`：固定官方源码与对应架构辅助程序的 SHA-256，完整校验后提取运行白名单，不执行上游安装脚本。使用私有暂存目录、安装锁和同卷排他发布；失败只清理本次暂存目录，已有版本不覆盖。
- `Sources/TidyNest`：原生界面及界面状态；不能绕过 Core 执行任意命令。
- `Sources/TidyNestProtocol`：App 与独立引擎共享的结构化协议。
- `ContainerOwnership`：有界、无链接读取 Containers 直属元数据，以完整 `MCMMetadataIdentifier` 确认归属；枚举前后的全部正/负匹配证据都要复验，容器与文件路径不由显示名或 UUID 推测。应用安装仍须唯一，当前规则版本为 `mole-1.53.0-subset-5`。
- `FinderTrashService`：仅为已确认的权限受限应用本体发送固定 Finder Apple Event，使用公开 CF 文件身份与资源标识核验、600 秒回执等待；不使用脚本字符串路径拼接、sudo 或常驻服务。缺失/未知回执保留待核对结果，系统交互期间取消仅停止后续项并等待当前系统结果。
- `Sources/TidyNestEngine` / `Sources/TidyNestBridge`：固定规则、计划存储、同卷隔离移动、系统 Trash 与本地记录；不调用原版清理/卸载，不接受任意计划文件。
- `Sources/TidyNestEngine/Resources/MoleUpstream`：固定 1.53.0 的来源、GPL 许可和审计材料；不执行原始 shell 摘录。
- `Tests/TidyNestCoreTests`：解析失败、参数边界、取消和查询等可观察行为测试。
- `scripts`：本地构建及 App 打包；构建产物放 `build/`，临时验证放 `work/`。
- 中文界面和中文注释；注释解释原因。保持改动小且可验证。
- shell 命令使用 `rtk` 前缀；可用 `rtk proxy` 执行原命令。

## 验证

- 全部测试：`rtk proxy swift test --no-parallel`，当前默认实际执行 310 项（24 项原查询＋6 项列表缓存＋13 项单应用读取＋8 项安装＋29 项桥接＋10 项运行状态＋19 项操作记录存储＋100 项引擎＋101 项界面状态）。显式关闭测试并行，避免本机高负载触发现有脚本 fixture 的 3 秒启动窗口；不放宽生产超时或测试断言。另有 2 项系统废纸篓验证及 1 项联网安装验证默认跳过，需显式启用。
- 本地 App：`rtk proxy bash scripts/build-app.sh`，输出 `build/TidyNest.app`。
- 联网安装验收：`rtk proxy env TIDYNEST_VERIFY_MOLE_INSTALL=1 swift test --filter officialMoleInstallsAndRunsFromPublishedDirectory`。只在项目 `work/` 新建目录安装，并从最终目录检测版本、查询新建空目录；默认测试不联网。
- 必须区分编译检查、自动化测试和真实界面验证，不把未测项说成通过。
- 不使用真实应用或个人缓存作为删除测试数据；不自动升级 Mole、授予权限或请求 sudo。
- `mole uninstall --list` 仅用于列表，禁止把名字直接用于卸载；没有计划接口就没有删除按钮。
- M2 自动化执行只使用本任务创建的隔离 fixture，不确认真实个人缓存或已安装 App 的处理计划。首批动作只有移入废纸篓，不永久删除、不跨卷 copy+delete。
- `large_files` 可能缺省，未知占用与零占用不同；错误输出不能猜测为有效结果。
- 提交仅在需要时进行；先查 status/diff，只暂存相关文件。格式为 `[ai] <type>(<scope>): <中文说明>`。

## 本地使用与已验证范围

- 2026-09-11 可选数据库与会话文件：用户明确要求由使用者决定是否移除，默认不勾选。缓存/日志内原先因数据库扩展名、SQLite 文件头或会话等内容风险保留的普通文件进入可选清单，详情和最终确认显示登录/本地状态丢失及数据库配套文件说明。仅改规则与影响文案，沿用现有勾选/确认；编译模型、长期保护、白名单、归属、占用、身份、权限、链接及同卷校验不变，版本 subset-5，旧计划须重新检查。新增 5 项引擎回归（含 12 种路径参数）先红后绿；定向运行中一条新测试对长期保护提示层级的错误假设经保存计划定位后修正，生产保护未改。默认 310 项通过（101 XCTest＋212 Swift Testing 中 3 项 opt-in 跳过），只读代码审查无可行动问题。隔离原生真实引擎计划显示应用本体＋三份数据库、文件默认不选，单独勾选数据库后确认清单只有本体和该文件，风险文字可完整滚动查看；取消本体清除依赖，清理扫描三文件均未选且不可确认，样本内容全部保留。临时验收程序曾因误读引擎返回值崩溃，已改为从事件接收计划并完成验证，未修改正式代码以适配验收。release、安装后严格签名和二进制摘要核对通过，已备份更新 /Applications/TidyNest.app；正式启动恢复 72 项，新 Bridge capabilities 确认 subset-5。应用缓存、页面记录、引擎 History/Consumed/Journals 内容和时间均不变，未确认或执行真实移除。日志 work/optional-state-files-red.log、work/optional-state-files-full.log、work/optional-state-files-build.log；原生证据 work/optional-state-files-native-result.json，备份见 work/optional-state-files-install.json。真实 ZCode 新计划及 macOS 14 未实机验证。

- 2026-09-10 应用移除后列表同步：真实记录确认「云·绝区零」15:08 已由 Finder 移除，旧列表和缓存仍残留。仅在执行收尾后同步已确认本体结果，按精确路径更新内存和缓存，清失效详情、保留搜索与完整列表时间；引擎不变。新增 8 项回归，初始 7 项中 4 项先复现失败；审查补齐真实 Bridge 中断返回 unknown 且保留逐项结果的链路，复核无剩余问题。最终默认 305 项通过（101 XCTest＋207 Swift Testing 中 3 项 opt-in 跳过）。隔离原生窗口注入成功结果后返回应用页，2→1、详情清空、搜索保留、更新时间不变，分隔位置保持 507，缓存仅剩 B。release、安装后严格签名与摘要通过，已备份更新 /Applications/TidyNest.app，Bridge 二进制不变。通过正式刷新清除旧版残留后重开，搜索「云·绝区零」为 0 项；完整列表仍 72 项，本次刷新新增当前已安装的 Rebased。重开后缓存内容/时间、整个验证期间引擎 History/Consumed/Journals 均不变，未执行新的真实移除。证据 work/removal-sync-native-result.json，日志 work/removal-sync-red.log、work/removal-sync-full-final.log、work/removal-sync-build.log，备份见 work/removal-sync-install.json。macOS 14 未实机验证。

- 2026-09-10 包装应用 unknown 标识重检修复：真实缓存中「云·绝区零」标识为 `unknown`，旧版重新检查仍原样送检，原生复现标识不一致。维护请求在现有互斥与取消范围内复用安全单项读取补全 unknown，再由引擎独立核验；保持外层路径，已有真实标识冲突不自动替换，不改列表缓存与引擎规则。新增 4 项桥接回归，其中 2 项先复现失败；相关 70 项与默认完整 297 项通过（93 XCTest＋207 Swift Testing 中 3 项 opt-in 跳过），限定只读审查无可行动问题。release、安装后严格签名与摘要核对通过，已备份更新 /Applications/TidyNest.app；Bridge 随 Core 链接重建。正式启动恢复 72 项、不自动查询，12:10 直接从 unknown 缓存生成正确标识的 118.3 MB 本体计划、需要系统授权，已取消默认选择；12:11 原按钮重检继续使用正确标识并生成新计划，此次 helpd 文件映射占用触发现有保护，本体不可选，未放宽规则或关闭进程。首次容器提示为元数据读取受限，继续保留容器文件。目标 477 项身份/权限/时间、列表缓存内容/时间及引擎 History/Consumed/Journals 均不变，未确认或执行真实移除。证据 work/unknown-bundle-verification.json，红绿日志 work/unknown-bundle-red.log、work/unknown-bundle-targeted.log，全量 work/unknown-bundle-full.log，构建 work/unknown-bundle-build.log，备份见 work/unknown-bundle-install.json；macOS 14 未实机验证。
- 2026-09-10 磁盘双击目录：采用 List 的原生 primaryAction，单击保持详情选择，双击仅进入对应目录；只改 DiskView 与本文档。26 项 WorkspaceModel/ConcurrentDisk 回归通过，限定只读审查未发现可行动问题。隔离原生验证单击不查询、普通文件双击不查询、未选中文件夹行双击进入正确目标、图标双击再进一层、路径栏跨两层返回，请求无重复，选择前后分隔位置保持 529。右键菜单内容可见；菜单项及后续坐标操作受 CUA 元素失效/窗口读取错误限制，未声称本轮实测通过。release 与安装后严格签名/摘要核对通过，已备份更新 /Applications/TidyNest.app，Bridge 不变。正式启动恢复 72 项，启动后缓存和页面记录内容/时间不变；原目录选择后 CUA 持续超时，正式版双击复核未完成，1 秒进程采样主线程为正常事件等待。未执行真实移除，未重跑引擎全量，macOS 14 未实机验证。证据 work/disk-double-click-native-result.json，测试 work/disk-double-click-model-tests.log，构建 work/disk-double-click-build.log，备份见 work/disk-double-click-install.json。
- 2026-09-10 磁盘跨层导航：用 NSPathControl 与完整祖先菜单替换纯文本地址；仅改 DiskView 与本文档。26 项 WorkspaceModel/ConcurrentDisk 回归通过，限定代码审查无可行动问题，未重跑无关引擎全量。原生隔离验证中文/空格多层路径、单击跨三层、当前目录不重查、深目录→根目录→深目录、读取期间禁用与取消后恢复；980 点宽最近四层清晰，选择详情分栏保持 421。release 与安装后严格签名/摘要核对通过，已备份更新 /Applications/TidyNest.app，Bridge 二进制不变。正式启动恢复 72 项且未自动查询，缓存及页面记录内容/时间在启动后不变；正式导航返回截图原目录后显示 144 KB/26 文件，分隔位置仍 531，祖先菜单完整。正式选择过程中曾收到文件路径并被原查询校验拒绝，随后点击其上层路径成功恢复；未修改选择框逻辑、未读取文件正文或移除数据。证据 work/disk-path-native-result.json，测试 work/disk-path-model-tests.log，构建 work/disk-path-build.log，备份见 work/disk-path-navigation-install.json。macOS 14 未实机验证。

- 运行 `rtk proxy bash scripts/build-app.sh` 后，双击 `build/TidyNest.app`。
- 每次启动进入「应用」页，检测 Mole 但不自动扫描。切到「应用」页可查看上次列表，不自动刷新；没有缓存时点击「读取应用」，已有缓存时可选中目标直接「检查移除计划」；需要更新显示信息时，再点击「刷新列表」或「刷新此应用」。磁盘查询仍需主动选择文件夹。
- 未安装时，可在主界面或设置中点击「安装 Mole 1.53.0」。安装位置为 `~/Library/Application Support/TidyNest/Mole/1.53.0/mole`，仅供本 App 自动发现，不修改 shell 配置。优先复用 `/opt/homebrew/bin`、`/usr/local/bin`、`~/.local/bin` 下的已有 Mole；检测错误和不支持的版本不会被当成缺失。
- 安装来源为[官方固定源码](https://github.com/tw93/Mole/tree/V1.53.0)与[官方发布摘要](https://github.com/tw93/Mole/releases/download/V1.53.0/SHA256SUMS)，源码摘要同时与 Homebrew 对应版本核对。无 Homebrew、sudo、自动升级或 latest 回退；网络错误可重试。
- 2026-09-09 系统授权移除扩展：用户明确同意后，权限受限且其他核验通过的应用本体可勾选，确认时由 Finder 接管系统移除；原应用与 Bridge 不提权、不改权限。新增 19 项回归，默认 293 项通过（93 XCTest＋203 Swift Testing 中 3 项 opt-in 跳过）；审查中的缺失回执、日志失败与目标引用绑定问题已修复并复核。隔离原生 App→Core→Bridge→Finder 完整移入新建的 0555 包装应用，原 inode/权限/链接完整，未选缓存保留，操作记录正确；管理员密码与自动化授权窗口本次未触发。release、安装后严格签名与摘要核对通过，已备份更新 /Applications/TidyNest.app。正式启动恢复 72 项且不自动刷新，18:17 只读计划「云·绝区零」本体 118.3 MB、需要系统授权，勾选 1→0→1→0 与确认启用正常；保留未勾选，未确认真实移除。真实应用身份/权限、缓存内容/时间、引擎 History/Consumed/Journals 均不变。证据 work/authorized-removal-native-result.json，完整测试 work/authorized-removal-full.log，构建 work/authorized-removal-build.log，备份见 work/authorized-removal-install.json。旧计划需重检，真实 root 应用管理员授权移除及 macOS 14 实机留待用户验证。
- 2026-09-09 应用移动权限修复：原「云·绝区零」执行停在 moveIntent，root:wheel 0755 包无当前账户移动权限；隔离同条件 rename 复现 EACCES 13。新增 8 项引擎回归先红后绿，完整默认 274 项通过（92 XCTest＋185 Swift Testing 中 3 项 opt-in 跳过）；审查发现的 ACL 误判及正式复检中的权限/占用提示顺序均已修正并复核。release 与安装后严格签名、摘要核对通过，已备份更新 /Applications/TidyNest.app，Bridge 摘要更新，旧计划需重新生成。正式 16:59 只读计划显示明确权限原因、0 项已选、本体与确认禁用，Finder 入口可用。原应用身份/权限、列表缓存内容/时间及引擎 History/Consumed/Journals 集合/内容/时间均不变；页面记录仅新增本次只读检查，没有确认或执行真实移除、没有修改权限或关闭 helpd。证据 work/move-permission-native-result.json、work/move-permission-native/readonly-permission-result.json；完整日志 work/move-permission-full-verified.log，构建 work/move-permission-build.log，备份位置见 work/move-permission-install.json。macOS 14 未实机验证。
- 2026-09-09 确认弹窗退出修复：原生复现弹窗打开时 Quit 被 SwiftUI 默认策略拦截，退出代理未收到请求；采用系统 presentationPreventsAppTermination(false)，不替换退出代理、不改变任务收尾或执行逻辑。新 API 按 macOS 15.4 可用性判断，旧系统保留原视图；[Apple 官方说明](https://developer.apple.com/documentation/swiftui/view/presentationpreventsapptermination(_:))。本机 macOS 15.7.4 隔离原生菜单退出、Command-Q 共 3 次均收到代理请求、完成收尾并退出，未调用 apply。92 项界面状态测试通过，未重跑 Core/Engine 全量；release 与安装后严格签名、摘要核对通过，已备份更新 /Applications/TidyNest.app。正式重开恢复 72 项，缓存内容/时间、页面记录、引擎审计与 Bridge 不变。Dock 控制读取超时，右键菜单未直接实测；macOS 14 实机未验证。证据 work/quit-sheet-red.json、work/quit-sheet-native-result.json、work/quit-sheet-install.json，日志 work/quit-sheet-ui-tests.log、work/quit-sheet-build.log。
- 2026-09-09 确认图标误导修复：原确认清单使用 SF app 空轮廓，实际为静态图像；改为 NSWorkspace 应用图标，并将返回按钮明确为「返回调整选择」。只改展示，确认模型、勾选规则和引擎未变。17 项 MaintenanceModel 回归通过，未新增或重跑全量；隔离原生验证真实图标/缺失路径系统通用图标、单项/12 项清单、返回后取消本体 1→0 和确认入口同步禁用，未触发移除。release、安装后严格签名与摘要核对通过，已备份更新 /Applications/TidyNest.app；正式启动恢复 72 项，缓存内容/时间、页面记录、引擎审计及 Bridge 二进制不变。证据与备份见 work/confirmation-icon-native-result.json、work/confirmation-icon-install.json；日志 work/confirmation-icon-tests.log、work/confirmation-icon-build.log。
- 2026-09-09 应用与操作记录并发：应用列表/单项刷新可与维护检查双向并行，记录读取改为独立任务和 Bridge 客户端，取消互不影响；检查期间可管理页面记录，执行与退出仍保留互斥。新增 14 项回归，初始 10 项中 9 项复现阻塞、1 项执行互斥通过；更新被新需求取代的旧串行断言后，266 项默认测试通过（92 XCTest＋177 Swift Testing 中 3 项 opt-in 跳过），限定只读审查无可行动问题。隔离原生窗口验证检查中列表 12→24 MB、单项 24→36 MB 且其他应用/完整列表时间不变，记录读取与清理分别取消，单条删除、清空精确两条、晚到结果保留、删除的执行记录不重新导入。release 与安装后严格签名和摘要核对通过，已备份更新 /Applications/TidyNest.app；正式重开进入应用、恢复 72 项、不自动刷新。应用缓存内容/时间、页面记录、引擎 History/Consumed/Journals 与 Bridge 二进制均不变。证据与备份位置见 work/app-history-concurrency-native-result.json、work/app-history-concurrency-install.json；全量日志 work/app-history-concurrency-full-verified.log，构建 work/app-history-concurrency-build.log。未执行真实文件移除或真实记录删除，未测真实 Mole/Bridge 同时读取的耗时。
- 2026-09-09 清理检查与磁盘并行：新增 10 项状态回归先复现阻塞后通过，覆盖双向启动、取消和失败结果隔离、派发前取消磁盘不拖住检查、执行等待磁盘/取消收尾及退出同时取消。252 项默认测试通过（78 XCTest＋177 Swift Testing 中 3 项 opt-in 跳过），限定只读审查无阻塞问题。复用生产界面的隔离原生 App 验证清理中选择文件夹、两种启动顺序确实重叠、磁盘取消不停止检查、扫描期间显示结果和子目录，以及 5 条分开的操作记录。release 与安装后严格签名/摘要通过，已备份后更新 /Applications/TidyNest.app；正式 App 仅验证启动恢复 72 项和磁盘入口，未执行真实查询或移除。应用缓存内容/时间、页面记录、Bridge 二进制及引擎 History/Consumed/Journals 均不变。证据与备份：work/disk-concurrency-native-result.json、work/disk-concurrency-install.json；全量日志 work/disk-concurrency-full.log，构建 work/disk-concurrency-build.log。未做真实 Mole/Bridge 同时读取的耗时基准。
- 2026-09-09 应用页分栏修复：空态与详情使用稳定宿主，修复首次选中时分隔线从 509 跳到 626 的问题；隔离原生验证选择、切换、搜索空态与恢复保持 507，手动拖至 584 后仍稳定。release 和安装后严格签名通过，已备份并更新 /Applications/TidyNest.app；正式 72 项缓存列表同路径验证保持 508.5，手动拖至 573.5 后切换仍不变，验证后恢复原宽度。纯布局变更，本轮未重跑状态/引擎测试；Bridge 二进制、应用缓存内容/时间、页面操作记录及引擎 History/Consumed/Journals 均不变。记录与备份位置见 work/application-split-native-result.json、work/application-split-install.json，构建日志 work/application-split-build.log。
- 2026-09-09 分栏、操作记录和确认弹窗：242 项默认测试通过（68 XCTest＋177 Swift Testing 中 3 项 opt-in 跳过），限定审查的派发前取消问题已红绿修复。既有体积脚本并发超时，单项及显式串行全量复核通过，未改旧断言。隔离原生验收覆盖全部 6 类操作、失败/取消、单条删除和清空确认、刷新/重开不重现、手动拖动与选择分栏，以及单项/12 项/未知占用确认清单。release 和安装后严格签名通过，已更新 /Applications/TidyNest.app；Bridge 因新增 Core 模型链接重建，未修改引擎规则。正式原目录读取 123.4 MB/85 文件，选择前后分隔位置 531 不变；操作记录立即新增磁盘读取，选择详情前后位置 408 不变，旧执行结果仍在。应用缓存内容/时间及引擎 History/Consumed/Journals 不变，未删除真实记录或移动真实应用/缓存。证据：work/history-expansion-full-verified.log、work/history-expansion-native-result.json、work/history-expansion-install.json；备份：/Users/yuqiqian/Utils/AI/codex-workspace/tidy-nest/work/app-install-backups/85081d14-80f9-44b2-a73d-2ca7d48a4111/TidyNest-before-history-expansion.app。
- 2026-09-08 检查提示紧凑布局：短内容按实际高度展开，路径与原因按条分组，超过 120 点才滚动；整行点击与勾选交互不变。980 点宽原生隔离窗口验证一条短提示无多余滚动区、12 条长提示可滚至最后一条且内容完整，长转短及新计划折叠正常。纯样式改动，未新增或重跑状态/引擎测试；release、安装后严格签名与主程序摘要核对通过，引擎二进制未变。已更新 `/Applications/TidyNest.app`；正式「云·绝区零」只读计划展开显示紧凑两行，保持未勾选和确认禁用，未执行真实移除，缓存内容与修改时间不变。记录 `work/issue-layout-native-result.json`，构建日志 `work/issue-layout-build.log`；备份 `work/app-install-backups/86299c12-c8e0-4e56-86b7-7aba079b8d03/TidyNest-before-compact-issues.app`。
- 2026-09-08 计划页交互修复：检查提示整行可点击展开/收起，显示操作文字和辅助功能状态；应用本体允许取消勾选，取消时同步清除依赖残留和旧确认，空选择不能确认，重新勾选不自动恢复残留。引擎二进制及必选/依赖规则未变。原窗口复现文字处无法展开与本体 checkbox 禁用；两项状态回归先失败后通过，默认实际 210 项全部通过，3 项 opt-in 未启用，限定审查通过。release、安装后严格签名和摘要核对通过，已更新 `/Applications/TidyNest.app`。正式窗口验证提示文字点击展开、行内留白点击收起；「云·绝区零」勾选 1→0→1，「取消当前选择」清为 0，确认入口同步禁用。未打开确认或执行真实移除，列表缓存内容及修改时间不变。日志 `work/plan-interaction-full.log`、记录 `work/plan-interaction-native-result.json`；备份 `work/app-install-backups/53f26472-33a3-4b03-9336-3a13bf6de2f5/TidyNest-before-plan-interaction.app`。
- 2026-09-08 占用提示与重新检查优化：显示占用者、PID、访问方式与路径，计划页可直接重新检查同一应用；只容忍整包本体的明确外部只读普通文件访问，目录、写入、映射、锁和未知状态继续保留，规则版本 subset-4。默认实际 209 项全部通过，3 项 opt-in 未启用；原有 3 秒超时用例单独复核及全量重跑通过，未修改断言。release、安装后严格签名与二进制摘要核对通过，已更新 `/Applications/TidyNest.app`。隔离原生验收确认真实目录占用的进程/PID/路径和禁用状态，释放后点击重检，同一目标恢复可选；真实系统 lsof 与包装引擎隔离移动验证通过。正式窗口未刷新即可检查「云·绝区零」，本次本体通过核验且核对入口启用，无占用提示；容器缓存/日志因归属元数据读取受限继续保留。未执行真实移除或关闭其他应用，72 项缓存内容与修改时间不变；记录 `work/occupancy-native-result.json`，日志 `work/occupancy-full-verified.log`。旧版备份为 `work/app-install-backups/a1a234a7-3f61-458b-ba59-236e8fb360da/TidyNest-before-occupancy-improvement.app`。
- 2026-09-08 缓存应用直接检查：取消本会话必须先刷新单项或完整列表的界面门槛，选中缓存应用即可进入独立维护检查；刷新仍用于更新显示信息。精确记录匹配、忙碌/取消收尾/退出互斥与引擎身份核验保留。新增入口回归先失败后通过，默认实际 196 项全部通过，3 项 opt-in 未启用；release、安装后严格签名与二进制摘要核对通过，已更新 `/Applications/TidyNest.app`。正式窗口重开恢复 72 项，未点击任何刷新即可检查「云·绝区零」并返回对应本体计划；占用保护仍正常保留目标，未执行真实移除。返回应用页后选中项保留，缓存内容与修改时间均未变；记录见 `work/direct-plan-native-result.json`，全量日志 `work/direct-plan-full.log`。旧版备份为 `work/app-install-backups/9a479333-35e5-477d-a549-3078dad458ad/TidyNest-before-direct-plan.app`。
- 2026-09-08 包装应用维护扩展：支持已知包装布局的完整外层本体，以及唯一安装、可确认归属的全局或单应用容器缓存/日志；文稿、存档所在的 Documents/Application Support、偏好与共享容器保留。新增 16 项引擎回归，默认实际 195 项全部通过，3 项 opt-in 未启用。审查指出的移动边界重复容器、扫描负匹配证据变化与取消收尾均有红绿验证；release、安装后严格签名与二进制摘要核对通过，已更新 `/Applications/TidyNest.app`。真实「云·绝区零」单项刷新和只读计划确认新包装本体规则生效，未执行真实移除；当前被 QSpace Pro 打开外层应用目录、helpd 打开包内 InfoPlist.strings 的占用保护阻止，释放占用后的真实勾选仍待验收。独立只读快照两次匹配，477 个成员，记录 71,715 字节，估算内容 118,250,813 字节；日志 `work/wrapped-native-snapshot.log`。本机 Containers 元数据只读探查受 EPERM 限制，未扩大权限，真实容器缓存清理未验收；隔离样本覆盖选择、存档/未选文件保留、归属/父链/元数据变化、链接和取消。旧版备份为 `work/app-install-backups/c34161d8-9cb5-4c30-bd55-a0bfdcb1a4e1/TidyNest-before-wrapper-removal.app`。
- 2026-09-08 包装应用单项刷新修复：支持已知 `WrappedBundle → Wrapper/<名称>.app` 布局，读取真实名称/Bundle ID、统计外层占用，移除规则不变。179 项默认测试通过，3 项 opt-in 未启用；release 与安装后严格签名检查通过，已更新 `/Applications/TidyNest.app`。正式原生窗口对「云·绝区零」单项刷新成功，显示 `com.miHoYo.cloudgames.Nap` 和 119.1 MB，其他 71 项及完整列表时间不变，选中项保留；未执行维护或移除。隔离测试覆盖越界/路径链接、损坏或过大信息、元数据/包装/载荷变化及取消。首次新增链接读取因 C 缓冲区传递问题崩溃，按堆栈改为显式 CChar buffer 后相关与全量回归通过。旧版备份为 `work/app-install-backups/486646c0-e891-4dd5-acd8-ef3bc55c990a/TidyNest-before-wrapper-refresh.app`，原生缓存核对结果在 `work/wrapped-refresh-native-result.json`。
- 2026-09-08 默认应用页与 Xcode 移除计划修复：启动固定进入「应用」，保留手动刷新。多副本仅授权所选路径本体；Xcode 仅本体，共享缓存和开发日志保留。177 项默认测试通过，3 项 opt-in 未启用；release 与安装后严格签名检查通过，已更新 `/Applications/TidyNest.app`。原生验证从清理计划页退出重开进入应用，列表缓存内容和修改时间不变。真实 Xcode 只读快照两次匹配，135,501 个成员完整保存/读回，记录 29,629,273 字节；测试脚本和日志在 `work/xcode-native-snapshot-verification.swift`、`work/xcode-native-snapshot.log`。隔离测试确认多副本保留、包内硬链接整体移动、包外链接/共享组写/危险模式拦截及大型记录无损读回。正式原生计划已消除重复标识提示，但当前被 Raycast、QSpace Pro 打开 Xcode 包内文件/目录的运行状态保护拦截，释放占用后的真实勾选验收待完成；未移除真实 Xcode、未退出占用程序、未验证 root 属主应用实际移入系统 Trash。旧计划需重新生成。旧版备份为 `work/app-install-backups/a5be2e04-50ce-4dcb-a045-a72869b37526/TidyNest-before-xcode-removal-fix.app`。
- 2026-09-08 维护进度优化：检查与执行改为横向进度卡片，检查显示当前应用名称、序号和最终复核阶段；执行按已选项目返回的独立结果计算已处理数量，最终结果到达前仍保持执行状态。166 项默认测试通过，3 项 opt-in 未启用；release 与安装后严格签名检查通过，已更新 `/Applications/TidyNest.app`。复用生产界面的隔离原生验收确认流动横条、阶段变化、取消后恢复入口，以及注入结果的 0/2→1/2 和最终保留状态；100% 等待最终结果与取消期间计数由自动化测试覆盖。正式 App 已验证只读检查进度条及取消，未执行真实移除；未改变扫描范围或核验规则，未测耗时变化。旧版备份为 `work/app-install-backups/4286a25e-ce9f-4239-9758-f13dd770fe61/TidyNest-before-maintenance-progress.app`。
- 2026-09-08 单应用刷新：详情增加「刷新此应用」，只读取目标名称、标识与包体积，按本会话具体记录放行移除计划入口。163 项默认测试通过，3 项 opt-in 未启用；release 与安装后严格签名检查通过，已更新 `/Applications/TidyNest.app`。隔离原生验收确认 A 更新/B 不变、切换选择后取消 A 仍准确显示、缓存仅替换 A；正式 App 只读刷新拾净自身后检查入口启用，其他 71 项和完整列表时间不变，未执行真实移除。取消用例初次因固定 1 秒就绪窗口失败，改为等待完整 PID 并确保取消收尾后通过全量回归。移除引擎未改，计划仍需安装集合核验，未做精确耗时基准。旧版备份为 `work/app-install-backups/39aa24e8-f6eb-4020-bc2a-3564994e9b85/TidyNest-before-single-application-refresh.app`。
- 2026-09-08 连接提示优化：普通检测进度与取消入口移至侧栏，初始/检测/取消收尾不显示顶部横幅，异常与安装进度保留。143 项默认测试通过，3 项 opt-in 未启用；release 与安装后严格签名检查通过，已更新 `/Applications/TidyNest.app`。复用生产界面的临时 App 延迟只读检测 12 秒，原生验证检测前后主内容位置稳定、侧栏取消/重试及检测中浏览缓存列表；本机正式 App 重开确认采用侧栏提示。此次只优化展示，未测检测耗时变化。旧版备份为 `work/app-install-backups/c37aa7df-2d07-4d9a-9020-9ef79e46a724/TidyNest-before-quiet-mole-detection.app`。
- 2026-09-08 启动页修复：每次新启动固定进入「清理」页，不持久化页面，不自动扫描。142 项默认测试通过，3 项 opt-in 未启用；release 与安装后严格签名检查通过，已更新 `/Applications/TidyNest.app`。原生验证从「应用」页退出重开回到「清理」页，应用缓存内容及修改时间不变。旧版备份为 `work/app-install-backups/15a797f6-7c2d-418c-a7ae-fbbf2610efb3/TidyNest-before-default-clean-page.app`。
- 2026-09-08 手动刷新调整：按用户要求取消启动/重新检测/安装完成后的自动应用查询；进入仅恢复快照，需要主动点击读取或刷新。142 项默认测试通过，release 与安装后签名核对通过，已更新 `/Applications/TidyNest.app`。原生窗口验证启动和切页返回均显示上次 72 项列表，没有更新进度，缓存内容与修改时间不变。旧版备份为 `work/app-install-backups/6da627c3-c17d-4202-88a6-a6e13dc43b17/TidyNest-before-manual-refresh.app`。
- 2026-09-08 身份诊断修复：区分安装检查未找到应用与应用仍在但列表标识不一致；Mole 返回 unknown 的 iPhone/iPad 包装应用明确提示当前不支持移除或清理，严格身份校验和包装应用保护不变。检查未完成且无项目时显示警示空态。142 项默认测试通过，release/安装后签名检查通过；已更新 `/Applications/TidyNest.app`。原生窗口对截图中的包装应用只生成检查结果，确认准确限制提示、无候选、确认按钮禁用，未执行真实移除。
- 2026-09-08 应用加载优化：139 项默认测试通过，3 项 opt-in 未启用；release 打包和严格签名检查通过，已更新 `/Applications/TidyNest.app`。真实原生窗口验证自动读取 72 个应用、退出重开先显示快照并后台更新、更新中搜索和详情可用、取消保留搜索与选中项；更新未成功前停用移除检查。旧版备份为 `work/app-install-backups/6b4d2dd5-d822-4382-be08-16392ef055f7/TidyNest-before-application-cache.app`。本次未修改 Mole 扫描逻辑，未做精确耗时基准。
- 2026-09-07：本机 macOS 15.7.4 / Apple Silicon / Mole 1.53.0 编译、105 项全量测试基线通过；最后追加 lsof 分类用例后24项相关回归通过，默认套件增为106项。最终0.2.0 release 打包及严格签名结构检查通过。
- 2026-09-07 自动安装增量：124 项默认测试实际通过，3 项 opt-in 默认跳过；另单独启用的官方联网安装测试通过，已从最终安装目录重新检测 1.53.0 并查询中文及空格路径下的空目录。安装测试均位于 `work/`，未替换本机已有的 Mole。
- 自动安装原生验收：复用生产界面的临时 App 仅注入隔离检测/安装目录，验证缺失按钮、点击下载、取消后无暂存/半成品、重新安装、成功自动连接和设置显示真实版本/路径。临时 App 不包含维护引擎，不作为 M2 验收。最终 0.3.0 release 严格签名通过，已更新 `/Applications/TidyNest.app`；原生启动与设置确认继续使用 `/opt/homebrew/bin/mole`，已有安装时不显示安装按钮，维护引擎保护设置读取正常。旧 App 备份在 `work/app-install-backups/`。
- 真实窗口验证：M1 应用读取、搜索、详情、设置重检、取消、目录选择、中文及空格路径下钻、返回上层、Finder 定位；读取中退出后未遗留本次查询进程。M2 已验证真实扫描、默认不选、单项选择、详情、准确确认清单、保护增删与计划失效、只读检查取消与明确强制结束。专用测试App及所选缓存从原生窗口移入废纸篓，未选缓存身份和内容不变；执行中等待完成后退出及重新打开后的结果明细通过。
- 独立系统Trash烟测已验证新建所选文件的移动及内容；原生GUI验收中，事后Python读取Trash内缓存A被EPERM拒绝，该独立内容比对未完成，没有扩大权限。原生引擎最终快照、测试App成员hash及未选B指纹均通过；细节见 `docs/m2-plan.md`。
- 空目录、普通文件、大文件及被环境变量干扰的目录查询已用真实 Mole 验证；细节见 `docs/m1-plan.md`。
- 当前 App 为本机架构的 ad-hoc 签名构建；macOS 14 实机、Intel、正式发行签名/公证与受隐私权限保护的目录尚未验收。
- M2 缓存/日志只处理完整 bundle ID 目录或已确认包装应用容器内指定 Caches/Logs 中枚举的普通文件；应用本体限标准 Applications 目录，残留限上述文件。安装唯一性核验覆盖 Mole 固定 8 个安装根目录及 `/Volumes`，不自动扫描桌面/下载，也不证明范围外不存在未启动副本。
- 首批只有移入废纸篓，没有永久删除或自动恢复；不会立即释放空间。Finder「放回原处」可能返回私有暂存目录，应按操作记录中的原路径手动移回。
- M1 查询沿用 Mole 自身行为，可能更新其缓存或日志；M2 原始 Mole shell 摘录仅供审计，不执行完整 clean/uninstall。
